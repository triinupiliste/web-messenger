# Public URL used in links sent by email. In production, use your HTTPS API URL.
APP_BASE_URL=http://localhost:5000/api

# Brevo transactional email credentials.
BREVO_API_KEY=replace_with_your_brevo_api_key
MAIL_FROM=verified-sender@example.com
MAIL_FROM_NAME=Web & Mobile Messenger

# Required by the backend (see docker-compose.yml for local defaults). Must be
# set to a strong, permanent value in any real deployment (e.g. Railway) —
# changing ENCRYPTION_KEY after users have registered makes their existing
# encrypted data unreadable.
JWT_SECRET=replace_with_a_long_random_string
ENCRYPTION_KEY=replace_with_a_long_random_string

# Cloudflare R2 (S3-compatible object storage) — required. The backend fails
# to start without these; media uploads (images, videos, voice notes, avatars)
# are stored here rather than on local disk, since local files don't survive
# a Railway redeploy. Create a bucket and an API token in the Cloudflare
# dashboard (R2 > Manage API Tokens).
R2_ACCOUNT_ID=replace_with_your_cloudflare_account_id
R2_ACCESS_KEY_ID=replace_with_your_r2_access_key_id
R2_SECRET_ACCESS_KEY=replace_with_your_r2_secret_access_key
R2_BUCKET_NAME=replace_with_your_r2_bucket_name
